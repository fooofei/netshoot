sudo: required
services:
  - docker

# 不可以是 generic 不然不会升级 Go 版本
language: go

go:
  - 1.15.x

script:
  - bash ./scripts/build.sh

# deploy:
#   - provider: script
#     script: bash ./scripts/docker_push.sh
#     on:
#       branch: x86_64

arch:
  - amd64
os: linux 
