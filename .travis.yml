sudo: required
services:
  - docker

language: generic

script:
  - docker build . -t fooofei/netshoot-aarch64:latest
after_script:
  - docker images

deploy:
  - provider: script
    script: bash ./scripts/docker_push.sh
    on:
      branch: aarch64
arch:
  - arm64
os: linux 
